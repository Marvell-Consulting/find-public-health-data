{
  "$schema": "https://biomejs.dev/schemas/2.5.3/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "includes": ["**", "!**/drizzle"]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "style": {
        "noRestrictedImports": {
          "level": "error",
          "options": {
            "paths": {
              "@fphd/public-web": "Deployable apps must never import one another (ADR006). Move the shared code into a packages/* workspace package.",
              "@fphd/internal-web": "Deployable apps must never import one another (ADR006). Move the shared code into a packages/* workspace package.",
              "@fphd/public-api": "Deployable apps must never import one another (ADR006). Move the shared code into a packages/* workspace package.",
              "@fphd/internal-api": "Deployable apps must never import one another (ADR006). Move the shared code into a packages/* workspace package."
            }
          }
        }
      }
    }
  },
  "assist": {
    "enabled": true,
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "semicolons": "always",
      "trailingCommas": "all"
    }
  }
}
